{
  "name": "sticky-todo",
  "private": true,
  "type": "module",
  "version": "1.1.0",
  "description": "轻量级桌面便签与待办清单",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "node --test \"src/*.test.js\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
